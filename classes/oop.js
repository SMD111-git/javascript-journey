const user={
    username:"SMD",
    logincount:8,
    signedin:true,
    getuserdeatils:()=>{
        console.log("got user details from database");
        
    }
}
console.log(user.username);
console.log(user.getuserdeatils());
